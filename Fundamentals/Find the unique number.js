const findUniq = function (arr) {
  const newArr = [];
  for (const i in arr) {
  }
};

const arr = [1, 0, 0];
console.log(arr.sort((a, b) => a - b));
console.log(arr[0] === arr[1] ? arr.pop() : arr[0]);
